{
  "name": "principalfish-site-tools",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "minify:electionmaps": "terser electionmaps/electionmaps.js --compress --mangle --output electionmaps/electionmaps.min.js && cleancss -O2 -o site/styles.min.css site/styles.css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.39.0"
  }
}
